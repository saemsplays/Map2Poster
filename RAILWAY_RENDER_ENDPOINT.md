# Railway `/render` Endpoint Implementation\n\nAdd this to your `app.py` file in the Railway project:\n\n```python\nimport os\nimport requests\nfrom flask import Flask, request, jsonify\nfrom create_verse_wallpaper import create_verse_wallpaper, get_coordinates\nimport traceback\n\napp = Flask(__name__)\n\n# Supabase configuration from environment\nSUPABASE_URL = os.getenv('SUPABASE_URL')\nSUPABASE_SERVICE_KEY = os.getenv('SUPABASE_SERVICE_ROLE_KEY')\n\ndef supabase_callback(status, render_id, result_path=None, error=None, worker_id='railway-worker-1'):\n    \"\"\"Call Supabase render-complete Edge Function\"\"\"\n    url = f\"{SUPABASE_URL}/functions/v1/render-complete\"\n    payload = {\n        'renderId': render_id,\n        'status': status,\n        'resultPath': result_path,\n        'error': error,\n        'workerId': worker_id\n    }\n    headers = {\n        'Authorization': f'Bearer {SUPABASE_SERVICE_KEY}',\n        'Content-Type': 'application/json'\n    }\n    try:\n        response = requests.post(url, json=payload, headers=headers, timeout=10)\n        return response.status_code == 200\n    except Exception as e:\n        print(f\"Callback error: {e}\")\n        return False\n\ndef upload_to_supabase_storage(file_path, render_id):\n    \"\"\"Upload file to Supabase Storage bucket 'renders'\"\"\"\n    try:\n        # Initialize Supabase client\n        from supabase import create_client\n        supabase = create_client(SUPABASE_URL, SUPABASE_SERVICE_KEY)\n        \n        # Read file\n        with open(file_path, 'rb') as f:\n            file_data = f.read()\n        \n        # Upload to storage\n        file_name = f\"{render_id}.avif\"\n        bucket_name = 'renders'\n        \n        result = supabase.storage.from_(bucket_name).upload(\n            file_name,\n            file_data,\n            {'content-type': 'image/avif'}\n        )\n        \n        # Get public URL\n        public_url = supabase.storage.from_(bucket_name).get_public_url(file_name)\n        return public_url\n    except Exception as e:\n        print(f\"Storage upload error: {e}\")\n        traceback.print_exc()\n        return None\n\n@app.route('/render', methods=['POST'])\ndef render_wallpaper():\n    \"\"\"Process render request from Supabase\"\"\"\n    try:\n        data = request.json\n        \n        # Extract parameters\n        render_id = data.get('renderId')\n        transaction_id = data.get('transactionId')\n        city = data.get('city', 'Nairobi')\n        country = data.get('country', 'Kenya')\n        theme = data.get('theme', 'noir')\n        preset = data.get('preset', 'mobile')\n        verse_text = data.get('verse', '')\n        verse_title = data.get('title', '')\n        distance = int(data.get('distance', 10000))\n        \n        print(f\"\\n=== Render Request ===\")\n        print(f\"Render ID: {render_id}\")\n        print(f\"City: {city}, {country}\")\n        print(f\"Theme: {theme}, Preset: {preset}\")\n        \n        if not render_id:\n            return jsonify({'error': 'Missing renderId'}), 400\n        \n        # 1. Notify Supabase: starting render\n        print(\"Notifying Supabase: processing...\")\n        supabase_callback('processing', render_id)\n        \n        # 2. Get coordinates\n        print(f\"Getting coordinates for {city}, {country}...\")\n        coords = get_coordinates(city, country)\n        \n        # 3. Generate wallpaper\n        print(\"Generating wallpaper...\")\n        output_path = create_verse_wallpaper(\n            city=city,\n            country=country,\n            point=coords,\n            dist=distance,\n            verse_text=verse_text,\n            verse_title=verse_title,\n            theme_name=theme,\n            preset=preset,\n            output_format='avif',\n            verse_position_name='center',\n            watermark=True,\n            quality=90,\n            dpi=100\n        )\n        \n        print(f\"Wallpaper generated: {output_path}\")\n        \n        # 4. Upload to Supabase Storage\n        print(\"Uploading to Supabase Storage...\")\n        storage_url = upload_to_supabase_storage(output_path, render_id)\n        \n        if not storage_url:\n            raise Exception(\"Failed to upload to storage\")\n        \n        # 5. Notify Supabase: completed\n        print(f\"Notifying Supabase: completed with URL {storage_url}\")\n        supabase_callback('completed', render_id, storage_url)\n        \n        # Cleanup local file\n        if os.path.exists(output_path):\n            os.remove(output_path)\n        \n        return jsonify({\n            'status': 'success',\n            'renderId': render_id,\n            'url': storage_url\n        }), 200\n        \n    except Exception as e:\n        error_msg = str(e)\n        print(f\"\\n!!! Render Error: {error_msg}\")\n        traceback.print_exc()\n        \n        # Notify Supabase: failed\n        if render_id:\n            supabase_callback('failed', render_id, error=error_msg)\n        \n        return jsonify({'error': error_msg}), 500\n\n@app.route('/', methods=['GET'])\ndef health():\n    return {\"status\": \"Map2Poster Engine Running\"}, 200\n\nif __name__ == '__main__':\n    port = int(os.getenv('PORT', 8080))\n    app.run(host='0.0.0.0', port=port)\n```\n\n## Required Python Dependencies\n\nAdd to `requirements.txt`:\n```\nsupabase==2.3.0\n```\n\n## Railway Environment Variables\n\nSet these in Railway dashboard:\n\n```\nSUPABASE_URL=https://nlzvpcyffinqkneptqcx.supabase.co\nSUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>\n```\n
